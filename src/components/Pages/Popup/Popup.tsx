import React, { FC } from 'react';
import { Typography } from '@mui/material';

// import '@fontsource/roboto';

export const Popup: FC<{}> = () => {
	return (
		<Typography>Hello from popoup!</Typography>
	);
};
